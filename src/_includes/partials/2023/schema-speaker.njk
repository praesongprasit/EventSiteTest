<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Event",
    "url": "{{ page.url | absoluteUrl(site.url) }}",
    "name": "{{ talkTitle }} by {{ title }}",
    "description": "{{ talkExcerpt }}",
    "startDate": "{{ talkStartTime }}",
    "endDate": "{{ talkEndTime }}",
    "image": {
      "@type": "ImageObject",
      "url": "{{ featuredImage | absoluteUrl(site.url) if featuredImage else site.image }}",
      "description": "{{ featuredImageAlt if featuredImageAlt else site.imageAlt }}",
      "width": "{{ featuredImageWidth if featuredImageWidth else site.imageWidth }}",
      "height": "{{ featuredImageHeight if featuredImageHeight else site.imageHeight }}"
    },
    "performer": {
      "@type": "Person",
      "name": "{{ fullName }}"
    },
    "location": {
      "@type": "Place",
      "name": "{{ EVENT_2023.location.name }}",
      "address": {
        "@type": "PostalAddress",
        "name": "{{ EVENT_2023.location.address.name }}",
        "streetAddress": "{{ EVENT_2023.location.address.streetAddress }}",
        "addressLocality": "{{ EVENT_2023.location.address.addressLocality }}",
        "postalCode": "{{ EVENT_2023.location.address.postalCode }}",
        "addressRegion": "{{ EVENT_2023.location.address.addressRegion }}",
        "addressCountry": "NZ"
      },
      "geo": {
        "@type": "GeoCoordinates",
        "latitude": "{{ EVENT_2023.location.geo.latitude }}",
        "longitude": "{{ EVENT_2023.location.geo.longitude }}"
      }
    },
    "superEvent": {
      {%- include 'partials/2023/schema.njk' -%}
    }
  }
</script>